import style from './divisor.module.scss'

const Divisor = () => {
    return(
        <div className={style.divisorContainer}>
            <div className={style.divisorContainer_img}>
                <div className={style.divisorContainer_opacty}></div>
            </div>
        </div>
       
    )
}

export default Divisor